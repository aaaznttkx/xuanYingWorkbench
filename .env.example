# Vercel + Neon Postgres Database URL
# 在 Vercel 上部署后，通过 Vercel Dashboard → Storage → Neon 集成自动设置
# 本地开发时（可选）：在 Neon 控制台创建数据库并复制连接字符串
DATABASE_URL=postgres://user:password@host/database?sslmode=require
